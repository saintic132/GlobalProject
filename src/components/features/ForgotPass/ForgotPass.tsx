import React from 'react';

type PropsType = {
    className?: string
}

export function ForgotPass(props: PropsType) {
    return (
        <div className={props.className}>
            ForgotPass
        </div>
    )
}